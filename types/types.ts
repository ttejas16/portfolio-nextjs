import { IconType } from "react-icons"

export type ProjectType = {
    name: string,
    description: string,
    icon: IconType,
    link: string
}

export type IconObject = {
    name: string,
    link: string,
    icon: IconType,
}
